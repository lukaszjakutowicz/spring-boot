# Spring boot on docker example

## Build project

To build project use maven command
```shell
mvn clean install 
```

## Run

Run application on docker environment using docker compose command

```shell
docker compose up --build
```